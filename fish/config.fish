if status is-interactive
    # Commands to run in interactive sessions can go here
end

# makeconf(){
# 	if [[ -n "$1" ]]
# 		then
#
# 		if [[ -n "$2" ]]
# 			then
# 			$2 $HOME/.config/$1/config.h
# 		fi
#
# 		cd $HOME/.config/$1
# 		sudo make clean install
# 		cd
#
# 	else
# 		cd $HOME/.config/dwm
# 		sudo make clean install
# 		cd $HOME/.config/dmenu
# 		sudo make clean install
# 		cd $HOME/.config/st
# 		sudo make clean install
#
# 	fi
# }

alias ls="eza --color=always -lah --icons=always"
set fish_greeting ""
export GTK_THEME=Flat-Remix-GTK-Magenta-Dark-Solid
